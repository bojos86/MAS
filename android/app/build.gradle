buildTypes {
    release {
        // عطّل resource shrinking
        shrinkResources false
        minifyEnabled false

        signingConfig signingConfigs.debug
    }
    debug {
        minifyEnabled false
    }
}
